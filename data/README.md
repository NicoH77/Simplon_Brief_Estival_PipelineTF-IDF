# Données

Placez le dataset **Banking77** ici sous le nom `banking77.csv`, au schéma
`text,label` (export depuis HuggingFace `PolyAI/banking77`).

Le code le charge automatiquement. En son absence, `data_loading.load()` lève
une erreur explicite : aucun jeu de substitution n'est fourni.
